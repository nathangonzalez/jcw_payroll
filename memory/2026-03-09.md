# 2026-03-09 - Session Notes

## CI/CD Pivot to VM Staging (1:1 with Prod)

- Replaced App Engine patch UAT lane with VM-native staging lane in `.github/workflows/ci-cd.yml`.
- New flow:
  - build-and-test
  - deploy-staging (VM service `labor-timekeeper-staging` on port `18080`, loopback bind)
  - uat-demo (GitHub Actions SSH tunnel to staging + Playwright)
  - uat-approval (manual gate)
  - promote (deploy same approved SHA to prod service)
- Workflow gates also allow `workflow_dispatch` so full staging/UAT/promote can be run manually (not only on push to `main`).

## Self-Hosted Runner Refactor (In Progress)

- Deploy/UAT/promote jobs now target GitHub self-hosted runner labels:
  - `self-hosted`, `Linux`, `X64`
- Removed SSH hop from workflow deploy path; jobs execute locally on VM runner.
- Added runner setup assets:
  - `scripts/setup_self_hosted_runner.sh`
  - `docs/self-hosted-runner.md`
- Updated `docs/vm-staging-rollout.md` to reflect self-hosted runner prerequisite.

## Firewall Hardening Status

- Public ingress on VM port `8080` was removed (rules `allow-timekeeper` and `allow-code-server` deleted in prior step).
- Verified direct public `:8080` blocked and public domain `https://payroll.jcwelton.com/api/health` still healthy.
- Added promote-time guard: fail if prod service is not loopback-only on `:8080`.

## Runtime Binding Hardening

- `labor-timekeeper/server.js` now supports `BIND_HOST` with safer defaults:
  - App Engine: `0.0.0.0`
  - VM production: `127.0.0.1`
- `.env.example` updated with `BIND_HOST`.

## VM Staging Runtime

- Created staging service unit on VM:
  - `labor-timekeeper-staging.service`
  - `WorkingDirectory=/home/nathan/dev/repos/jcw_payroll-staging/labor-timekeeper`
  - `EnvironmentFile=.env.staging`
- Staging data path:
  - `/home/nathan/dev/repos/jcw_payroll-staging/labor-timekeeper/data/staging/app.db`

## Backlog Additions Requested by Nate

- Added to `labor-timekeeper/SPRINT_STORIES.md`:
  - `US-5.9` Voice coding spike with Aider + `--voice`
  - `US-5.10` Custom Node.js voice coding agent prototype

## VM Outage Troubleshooting (Prod Proxy)

- Reproduced VM health split:
  - Local app healthy: `http://127.0.0.1:8080/api/health`
  - Public domain down: `https://payroll.jcwelton.com/api/health` (connection refused)
- Root cause was `caddy.service` failed after reboot with startup timeout.
- Verified Caddy config itself was valid (`caddy validate` passed).
- Restored service with:
  - `systemctl restart caddy`
  - Confirmed public health endpoint returned `200` JSON again.
- Added self-heal hardening on VM:
  - `/etc/systemd/system/caddy.service.d/override.conf`
  - `Restart=on-failure`
  - `RestartSec=5s`
  - `TimeoutStartSec=3min`
- Post-fix VM resources are healthy (not overloaded):
  - Mem used ~532Mi / 3.8Gi, disk 38%, load avg ~0.08 (1 min)

## Repo Hardening Follow-up

- Added VM script: `scripts/harden_caddy_service.sh`
- Added runbook: `docs/caddy-ops.md`
- Updated checklists:
  - `infra/deploy-checklist.md` now explicitly requires `caddy` active check
  - `docs/vm-staging-rollout.md` now references Caddy ops runbook

## UAT Demo Refresh (Weekly Payroll Policy)

- Updated admin UI to remove include-admin checkbox for weekly payroll prints.
- Weekly payroll print path now enforces admin-excluded behavior server-side.
- Reworked Playwright UAT demo:
  - slower step pacing for human review (`UAT_STEP_PAUSE_MS`, default 2500ms)
  - explicit AC-tagged on-screen annotations (`AC-1` .. `AC-5`)
  - added weekly flow visibility step (`All Entries`)
  - added weekly preview verification step (`Week of ...` cards)
- Added acceptance criteria doc:
  - `labor-timekeeper/UAT_ACCEPTANCE.md`

## Follow-up: Staging + Admin Monthly + Billing Disable

- Exposed staging via Caddy path route:
  - `https://payroll.jcwelton.com/staging/app`
  - `https://payroll.jcwelton.com/staging/api/health`
- Disabled billing surfaces in product flow:
  - `/api/export/monthly-billing` now returns `410`
  - `/api/admin/print-week-billing` now returns `410`
  - Admin UI billing buttons removed, replaced by admin monthly export button
- Added new admin monthly export endpoint:
  - `/api/export/monthly-admin?month=YYYY-MM`
  - output file `Admin_Monthly_<month>.xlsx`
- Updated weekly print ordering:
  - employee packets sorted by pay rate ascending (lowest to highest)
- Updated UAT spec:
  - added `AC-6` sorted-by-rate verification
  - increased demo pacing via `UAT_STEP_PAUSE_MS` in CI
