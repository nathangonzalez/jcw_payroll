# 2026-03-09 - Session Notes

## CI/CD Pivot to VM Staging (1:1 with Prod)

- Replaced App Engine patch UAT lane with VM-native staging lane in `.github/workflows/ci-cd.yml`.
- New flow:
  - build-and-test
  - deploy-staging (VM service `labor-timekeeper-staging` on port `18080`, loopback bind)
  - uat-demo (GitHub Actions SSH tunnel to staging + Playwright)
  - uat-approval (manual gate)
  - promote (deploy same approved SHA to prod service)

## Firewall Hardening Status

- Public ingress on VM port `8080` was removed (rules `allow-timekeeper` and `allow-code-server` deleted in prior step).
- Verified direct public `:8080` blocked and public domain `https://payroll.jcwelton.com/api/health` still healthy.
- Added promote-time guard: fail if prod service is not loopback-only on `:8080`.

## Runtime Binding Hardening

- `labor-timekeeper/server.js` now supports `BIND_HOST` with safer defaults:
  - App Engine: `0.0.0.0`
  - VM production: `127.0.0.1`
- `.env.example` updated with `BIND_HOST`.

## VM Staging Runtime

- Created staging service unit on VM:
  - `labor-timekeeper-staging.service`
  - `WorkingDirectory=/home/nathan/dev/repos/jcw_payroll-staging/labor-timekeeper`
  - `EnvironmentFile=.env.staging`
- Staging data path:
  - `/home/nathan/dev/repos/jcw_payroll-staging/labor-timekeeper/data/staging/app.db`

## Backlog Additions Requested by Nate

- Added to `labor-timekeeper/SPRINT_STORIES.md`:
  - `US-5.9` Voice coding spike with Aider + `--voice`
  - `US-5.10` Custom Node.js voice coding agent prototype
